<UserControl x:Class="CourseProgect_Planeta35.Controls.InventoryListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:CourseProgect_Planeta35.Models"
             Background="Transparent">

    <Grid>

        <!-- 🌑 фон -->
        <Grid>
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#1A1F1C" Offset="0"/>
                        <GradientStop Color="#2B3A31" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Rectangle Opacity="0.2">
                <Rectangle.Fill>
                    <RadialGradientBrush Center="0.2,0.2"
                                         RadiusX="0.9"
                                         RadiusY="0.9">
                        <GradientStop Color="#8B9B4C" Offset="0"/>
                        <GradientStop Color="#1A1F1C" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>

        <!-- 🧊 UI -->
        <Grid Margin="20">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Margin="0 0 0 14">
                <TextBlock Text="Инвентарь"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="White"/>

                <TextBlock Text="Управление и контроль всех активов"
                           Foreground="#BFCBC2"
                           Margin="0 4 0 0"/>
            </StackPanel>

            <!-- FILTER BAR -->
            <Border Grid.Row="1"
                    Background="#1E2421"
                    CornerRadius="14"
                    Padding="14"
                    BorderBrush="#2F3B33"
                    BorderThickness="1"
                    Margin="0 0 0 14">

                <WrapPanel>

                    <!-- SEARCH -->
                    <TextBox x:Name="SearchBox"
                             Width="300"
                             Height="36"
                             Margin="0 0 12 0"
                             Background="#2A322D"
                             Foreground="White"
                             BorderThickness="0"
                             VerticalContentAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>

                    <!-- CATEGORY -->
                    <ComboBox x:Name="CategoryFilter"
                              Width="180"
                              Margin="0 0 12 0"
                              Background="#2A322D"
                              Foreground="White"
                              BorderThickness="0"
                              SelectionChanged="CategoryFilter_SelectionChanged"/>

                    <!-- STATUS -->
                    <ComboBox x:Name="StatusFilter"
                              Width="180"
                              Margin="0 0 12 0"
                              Background="#2A322D"
                              Foreground="White"
                              BorderThickness="0"
                              SelectionChanged="StatusFilter_SelectionChanged"/>

                    <!-- ADD -->
                    <Button Content="➕ Добавить"
                            Width="140"
                            Background="#8B9B4C"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="AddButton_Click"/>

                </WrapPanel>
            </Border>

            <!-- CONTENT -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto">

                <StackPanel>

                    <TextBlock x:Name="SubTitleText"
                               Text="0 объектов найдено"
                               Foreground="#BFCBC2"
                               Margin="0 0 0 12"/>

                    <ItemsControl x:Name="ItemsGrid">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:InventoryItem}">

                                <!-- CARD -->
                                <Border Margin="8"
                                        Width="260"
                                        Padding="12"
                                        Background="#1E2421"
                                        CornerRadius="14"
                                        BorderBrush="#2F3B33"
                                        BorderThickness="1">

                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="#8B9B4C"/>
                                                    <Setter Property="Background" Value="#232B26"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <StackPanel>

                                        <Image Source="{Binding ImagePathToShow}"
                                               Height="120"
                                               Stretch="UniformToFill"
                                               Margin="0 0 0 10"/>

                                        <TextBlock Text="{Binding Asset.Name}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="White"
                                                   TextTrimming="CharacterEllipsis"/>

                                        <TextBlock Text="{Binding Asset.Category.Name}"
                                                   Foreground="#BFCBC2"
                                                   FontSize="12"
                                                   Margin="0 4 0 0"/>

                                        <TextBlock Text="{Binding Asset.Department.Name}"
                                                   Foreground="#9AA59F"
                                                   FontSize="12"
                                                   Margin="0 2 0 0"/>

                                        <!-- STATUS -->
                                        <Border Margin="0 8 0 0"
                                                Padding="6 3"
                                                CornerRadius="6"
                                                Background="#8B9B4C"
                                                HorizontalAlignment="Left">

                                            <TextBlock Text="{Binding Asset.Status}"
                                                       Foreground="White"
                                                       FontSize="12"/>
                                        </Border>

                                        <TextBlock Text="{Binding Asset.Cost, StringFormat={}{0:N0} ₽}"
                                                   Foreground="White"
                                                   FontSize="14"
                                                   Margin="0 8 0 0"/>

                                        <!-- ACTIONS -->
                                        <Button Content="Удалить"
                                                Margin="0 10 0 0"
                                                Background="#2A322D"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Click="DeleteButton_Click"/>

                                    </StackPanel>

                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </StackPanel>

            </ScrollViewer>

        </Grid>
    </Grid>
</UserControl>