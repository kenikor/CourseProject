<UserControl x:Class="CourseProgect_Planeta35.Controls.InventoryCheckControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:CourseProgect_Planeta35.Models"
             Background="#F5F1E9">

    <UserControl.Resources>
        <!-- Карточка -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Поисковая строка -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D2D5C4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="White" BorderBrush="#D2D5C4" BorderThickness="1" CornerRadius="10"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,0,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Бейдж статуса -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,2,0,6"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Background" Value="#8B9B4C"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <StackPanel>
            <!-- Поиск -->
            <TextBox x:Name="SearchBox"
                     Style="{StaticResource SearchBox}"
                     TextChanged="SearchBox_TextChanged"
                     Width="400"
                     FontSize="16"
                     Margin="0,0,0,10"/>

            <!-- Подзаголовок -->
            <TextBlock x:Name="SubTitleText" FontSize="15" Foreground="#3C2F1F" Margin="0,0,0,10"/>

            <!-- Карточки -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="500">
                <ItemsControl x:Name="ItemsGrid">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:InventoryItem}">
                            <Border Style="{StaticResource CardStyle}" MouseDown="Card_MouseDown">
                                <StackPanel Margin="12">
                                    <TextBlock Text="{Binding Asset.Name}" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Text="Категория:" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Asset.Category.Name}" Margin="0,0,0,6"/>
                                    <TextBlock Text="Статус:" FontWeight="Bold"/>
                                    <Border Style="{StaticResource StatusBadge}">
                                        <TextBlock Text="{Binding Asset.Status}" Foreground="White"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Детальная панель -->
            <Border Background="White" CornerRadius="12" Padding="12" Margin="0,10,0,0" Visibility="Collapsed" x:Name="DetailPanel">
                <StackPanel>
                    <TextBlock x:Name="ItemNameText" FontSize="16" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock x:Name="ItemCategoryText" FontSize="14" Foreground="Gray" Margin="0,0,0,10"/>
                    <TextBlock Text="Статус проверки:" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                        <RadioButton Content="Присутствует" GroupName="Status" Checked="Status_Checked" Tag="present"/>
                        <RadioButton Content="Отсутствует" GroupName="Status" Margin="10,0,0,0" Checked="Status_Checked" Tag="absent"/>
                        <RadioButton Content="Повреждено" GroupName="Status" Margin="10,0,0,0" Checked="Status_Checked" Tag="damaged"/>
                    </StackPanel>

                    <TextBlock Text="Примечания:" FontWeight="SemiBold"/>
                    <TextBox x:Name="NotesBox" AcceptsReturn="True" Height="80" Margin="0,5,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="Сохранить" Width="100" Click="SaveButton_Click"/>
                        <Button Content="Отмена" Width="100" Margin="10,0,0,0" Click="CancelButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
